make a gitignore after checking if exists 
add files and ai working documents to it 
such as .cogit folder if not added already
ignore attachments, logs, pycache 

make a readme after checking if exists 
make or update requirements.txt with current working versions after checking if exists

then execute 

git init 
git add . 
git commit -m "init"
git branch -M main
git remote add origin https://github.com/ai-enterpriseai/evolver.git
git push -u origin main
