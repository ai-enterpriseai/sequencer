# Sequencer - LLM Prompt Sequence Executor
# Install with: pip install -r requirements.txt

# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
openai==1.59.6
anthropic==0.37.1
huggingface-hub>=0.19.0
python-dotenv>=1.0.0
httpx==0.27.0

# Note: asyncio is built into Python 3.12, no need to install separately

